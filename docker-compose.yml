services:
  ebs:
    build: ebs
    restart: unless-stopped
    depends_on:
      - db
    ports:
      - 80:3000
    environment:
      DB_HOST: db
      DB_USER: ebs
      DB_PASSWORD: ebs
      DB_NAME: ebs

  db:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ebs
      MYSQL_USER: ebs
      MYSQL_PASSWORD: ebs
    volumes:
      - ./_volumes/db:/var/lib/mysql
